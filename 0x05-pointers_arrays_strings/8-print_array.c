#include "main.h"

/**
 * _strcpy - copies the string pointed to by src to dest
 * @dest: buffer
 * @src: pointer
 *
 * Return: always 0
 */

char *_strcpy(char *dest, char *src);
{
	int x;

	x = 0;
	if (src[x] != \0)
	{
		*src = *dest;
	}
	_putchar('\0')
	return(dest);
}
