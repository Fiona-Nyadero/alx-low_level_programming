#include "main.h"
#include <stdio.h>

/**
 * main - Prints the name
 * @argc: argument count
 * @argv: argument vector(array)
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void) argc;

	return (0);
}
