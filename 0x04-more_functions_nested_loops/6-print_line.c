#include "main.h"

/**
 * print_line - prints a straight line
 *
 *@n - parameter to print
 *
 * Return: Always 0.
 */

void print_line(int n)
{
	int n;
	n = 1
	if (n > 1)
	{	
		n++;
		_putchar(_);
	}
	else if (n < 1)
		_putchar('\n');
}
