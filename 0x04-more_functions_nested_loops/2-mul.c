#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a: parameter to multiply
 * @b: parameter to multiply
 *
 * Return: Always 0.
 */

int mul(int a, int b)
{
	putchar(a * b);
	return (0);
}
