#include "main.h"

/**
 * _sqrt_recursion - returns natural squareroot of a no.
 * @n: variable number
 *
 * Return: squareroot of n
 */

int _sqrt_recursion(int n)
{

	if (n < 0)
	{
		return (-1);
	}
	if (y == 0)
	{
		return (1);
	}
	return (x * _pow_recursion(x, y - 1));
}
